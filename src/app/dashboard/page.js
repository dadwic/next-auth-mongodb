import * as React from "react";
import AppWrapper from "@/components/AppWrapper";

export const metadata = {
  title: "Dashboard",
};

export default async function DashboardPage() {
  return <AppWrapper>Welcome!</AppWrapper>;
}
